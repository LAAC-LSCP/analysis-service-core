"""Core utilities for building analysis service plugins.

Provides the task lifecycle base class (`ModelPlugin`), environment
configuration, domain exceptions, filesystem helpers, logging, and
Redis integration.
"""
